{
  "name": "puresoftwares",
  "version": "1.0.0",
  "description": "Event Booking System",
  "main": "src/server.js",
  "author": "Development Team",
  "license": "ISC",
  "scripts": {
    "start": "node src/server.js",
    "migrate": "node scripts/migrate.js",
    "migrate:fresh": "node scripts/migrate-fresh.js",
    "setup": "npm install && node scripts/migrate.js",
    "test": "jest --coverage --verbose",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage --collectCoverageFrom='src/**/*.js' --collectCoverageFrom='!src/config/**'"
  },
  "dependencies": {
    "express": "^4.18.2",
    "sequelize": "^6.35.0",
    "mysql2": "^3.6.0",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "supertest": "^6.3.3"
  }
}
